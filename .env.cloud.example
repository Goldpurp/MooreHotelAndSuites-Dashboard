# Copy to .env.cloud and replace the example host with the shared Development
# deployment. This profile must never point to production data or credentials.
VITE_APP_ENV=development
VITE_API_MODE=direct
VITE_API_BASE_URL=https://api-development.moorehotelandsuites.com/api
VITE_API_TIMEOUT_MS=15000
VITE_DEV_PORT=3200
VITE_PREVIEW_PORT=4193
